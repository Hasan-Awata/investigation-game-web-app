Users (
    id PK, 
    username UNIQUE, 
    hashed_password, 
    XP INT, // Represents the player's current rank/level
    created_at
)

Cases (
    id PK, 
    title, 
    story, 
    min_player_XP INT, // Represents the required player level to unlock this case
    XP_on_solve INT // Represents how many XP the player gain when they finish the level.
    created_at
)

Levels (id, case_id, title, details, order_index)
Evidences (id, level_id, title, description, evidence_type, audio_url, img_url, paragraph)
Questions (id, level_id, text, img_url, msg_when_wrong)
Choices (id, question_id, text, is_correct)

GameRooms (id, case_id, host_user_id, invite_code, current_level_id, status)
RoomUsers (id, room_id, user_id, role, joined_at)
RoomVotes (id, room_id, user_id, question_id, choice_id)

CaseUser / UserCaseHistory (id, user_id, case_id, status, completed_at) -> Populated automatically when a room finishes a case.